% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_evaluate.R
\name{classify_dmrs}
\alias{classify_dmrs}
\title{Classify called DMRs against ground-truth spike-in regions}
\usage{
classify_dmrs(called_dmrs, truth_regions = NULL, min_overlap_bp = 1)
}
\arguments{
\item{called_dmrs}{data.table of called DMRs (chr, start, end, ...).}

\item{truth_regions}{data.table of spike-in regions (region_id, chr, start, end, ...).
If NULL, all called DMRs are treated as FP (null simulation).}

\item{min_overlap_bp}{Minimum overlap in base pairs to count as a match (default: 1).}
}
\value{
List with:
\item{classification}{data.table with each called DMR labeled TP or FP}
\item{fn_regions}{data.table of spike-in regions not recovered (FN)}
\item{summary}{Named vector: TP, FP, FN counts}
}
\description{
A called DMR is a true positive (TP) if it overlaps a spike-in region by
at least \code{min_overlap_frac} of the smaller region's width.
Spike-in regions without any overlapping DMR call are false negatives (FN).
Called DMRs without any overlapping spike-in are false positives (FP).
}
