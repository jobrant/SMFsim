% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_simulate_efficiency.R
\name{.betabinom_counts}
\alias{.betabinom_counts}
\title{Draw counts from a Beta-binomial (or Binomial) at a given mean rate}
\usage{
.betabinom_counts(mean_rate, size, s = NULL)
}
\arguments{
\item{mean_rate}{Numeric vector of target mean methylation rates.}

\item{size}{Numeric vector of coverages (recycled/aligned to \code{mean_rate}).}

\item{s}{Optional Beta precision; \code{NULL}/\code{Inf} gives a plain Binomial.}
}
\value{
Integer vector of methylated counts.
}
\description{
For each element, draws \code{Binomial(size, p)} where \code{p = mean_rate} when \code{s} is
\code{NULL}/\code{Inf}, or \code{p ~ Beta(mean_rate * s, (1 - mean_rate) * s)} when \code{s} is
finite (overdispersion, precision \code{s = alpha + beta}). Boundary means (0 or 1)
carry no dispersion and use \code{p = mean_rate} directly.
}
\keyword{internal}
